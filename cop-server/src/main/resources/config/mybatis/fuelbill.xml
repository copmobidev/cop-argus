<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cop.mobi.mycar.service.dao.FuelBillDao">
	<resultMap id="fuelBillResult" type="com.cop.mobi.mycar.entity.FuelBill">
		<result property="id" column="id" />
		<result property="uid" column="uid" />
		<result property="fuelType" column="fuelType" />
		<result property="charge" column="charge" />
		<result property="unitprice" column="unitprice" />
		<result property="lat" column="lat" />
		<result property="lng" column="lng" />
		<result property="addtime" column="addtime" />
	</resultMap>

	<insert id="addFuelBill">
		INSERT INTO LC_FuelBill
		(uid, fuelType, charge, unitPrice,
		lat, lng, addtime)
		VALUES
		(#{uid}, #{fuelType},
		#{charge}, #{unitPrice},
		#{lat}, #{lng}, #{addtime})
	</insert>

	<select id="getFuelBillById" resultMap="fuelBillResult">
		SELECT
		id, uid, fuelType,
		charge, unitPrice, lat, lng,
		addtime
		FROM LC_FuelBill
		WHERE id=${bid}
	</select>

	<select id="getFuelBillByAddtime" resultMap="fuelBillResult">
		SELECT
		id, uid,
		fuelType, charge, unitPrice, lat, lng,
		addtime
		FROM LC_FuelBill
		WHERE
		uid=${uid} AND addtime=#{addtime}
	</select>

	<select id="getFuellBills" resultMap="fuelBillResult">
		SELECT
		id, uid, fuelType, charge, unitPrice, addtime, lat, lng
		FROM
		LC_FuelBill WHERE uid=${uid}
		AND 
		<![CDATA[
		addtime>=${beginTime} AND addtime<=${endTime}
		]]>
	</select>

	<update id="updateFuelBill">
		UPDATE LC_FuelBill SET
		${updatecols}
		WHERE id=#{bid}
	</update>

	<delete id="deleteFuelBill">
		DELETE FROM LC_FuelBill WHERE id=#{bid}
	</delete>

</mapper>