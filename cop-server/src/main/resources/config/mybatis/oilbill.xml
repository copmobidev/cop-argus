<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cop.mobi.mycar.service.dao.OilBillDao">
	<resultMap id="oilBillResult" type="com.cop.mobi.mycar.entity.OilBill">
		<result property="id" column="id" />
		<result property="uid" column="uid" />
		<result property="oilType" column="oilType" />
		<result property="oil" column="oil" />
		<result property="unitprice" column="unitprice" />
		<result property="lat" column="lat" />
		<result property="lng" column="lng" />
		<result property="addtime" column="addtime" />
	</resultMap>

	<insert id="addOilBill">
		INSERT INTO LC_OilBill
		(uid, oilType, oil, unitPrice,
		lat, lng, addtime)
		VALUES
		(#{uid}, #{oilType},
		#{oil}, #{unitPrice},
		#{lat}, #{lng}, #{addtime})
	</insert>

	<select id="getOilBillById" resultMap="oilBillResult">
		SELECT
		id, uid, oilType,
		oil, unitPrice, lat, lng,
		addtime
		FROM LC_OilBill
		WHERE id=${bid}
	</select>

	<select id="getOilBillByAddtime" resultMap="oilBillResult">
		SELECT
		id, uid,
		oilType, oil, unitPrice, lat, lng,
		addtime
		FROM LC_OilBill
		WHERE
		uid=${uid} AND addtime=#{addtime}
	</select>

	<select id="getOilBills" resultMap="oilBillResult">
		SELECT
		id, uid, oilType, oil, unitPrice, addtime, lat, lng
		FROM
		LC_OilBill WHERE uid=${uid}
		AND 
		<![CDATA[
		addtime>=${beginTime} AND addtime<=${endTime}
		]]>
	</select>

	<update id="updateOilBill">
		UPDATE LC_OilBill SET
		${updatecols}
		WHERE id=#{bid}
	</update>

	<delete id="deleteOilBill">
		DELETE FROM LC_OilBill WHERE id=#{bid}
	</delete>

</mapper>